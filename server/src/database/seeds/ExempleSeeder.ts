import Knex from 'knex';

export async function seed (knex: Knex) {
  // const seedResult = await knex('table')
  //   .insert({
  //     value: '123'
  //   });

  // console.log(`Exemple Seeder: ${seedResult ? 'success' : 'error' }`)
}